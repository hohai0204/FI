@EndUserText.label: 'Access Control  Phiếu Chi'
@MappingRole: true
define role ZFA_C_PHIEUCHI {
    grant
        select
            on
                ZFA_C_PHIEUCHI
                    where
                        inheriting conditions from entity ZFA_R_PHIEUCHI;
                        
}